import React from 'react';

function LoginTitle({sentence}) {


      
        return (
        <div >
            <div className='login_title'>{sentence}</div>
        </div>
        );
      };

export default LoginTitle;